version: "3.9"

services:
  discord-bot:
    build: .
    container_name: gustavo-bot
    restart: unless-stopped

    env_file:
      - .env

    logging:
      options:
        max-size: "10m"
        max-file: "3"